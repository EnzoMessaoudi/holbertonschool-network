#!/usr/bin/env bash
#Script used to change the ip address of the localhost and facebook.com

sudo sed -i 's/^127\.0\.0\.1\s\+localhost/127.0.0.2 localhost/' /etc/hosts
sudo bash -c 'echo "8.8.8.8 facebook.com" >> /etc/hosts'
